---
import PageHeader from "../../components/PageHeader.astro";
import Layout from "../../layouts/Layout.astro";

const title = "NocoBase 服務夥伴";
const description =
  "發現遍佈全球的 NocoBase 認證服務夥伴，享受專業的技術支持和諮詢服務。";
const keywords = "NocoBase,夥伴,合作伙伴,技術支持,諮詢服務";

const partnersByContinent = [
  {
    continent: "亞洲",
    partners: [
      {
        id: "82",
        name: "上海攸遷信息科技有限公司 (SeaDocker)",
        location: "中國·上海",
        description:
          "SeaDocker 海多客是攸遷科技旗下跨境電商一站式交易服務平臺，提供從訂單聚合、倉儲物流到全球跨境服務的全流程支持。基於 NocoBase 構建多語言、多海外主體、多角色的超級跨境 OA 系統，實現財務、人事等全球協作管理。",
        logo: "https://service-cn.nocobase.com/storage/uploads/攸遷logo-u6g63u.svg",
        website: "https://www.seadocker.com/",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "企業管理系統", sub: ["企業資源計劃 (ERP)", "庫存與倉儲（WMS / Inventory）", "辦公自動化（OA）"] },
          { category: "行業解決方案", sub: ["零售/電商"] }
        ],
        contact: "leon@uoqian.com",
        cn: true,
        certified: true,
        featured: false
      },
      {
        id: "22",
        name: "鄭州光量子信息科技有限公司",
        location: "中國·鄭州",
        description:
          "光量子科技以零代碼技術賦能校園管理，實現降本增效。基於 NocoBase 平臺，為學校快速搭建教師榮譽登記與績效考核系統，實現全流程線上化。",
        logo: "https://service-cn.nocobase.com/storage/uploads/logo-94chjl.jpg",
        website: "",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          {
            category: "企業管理系統",
            sub: ["預約與排期系統", "業務流程管理 (BPM)", "內容管理系統 (CMS)", "客戶關係管理 (CRM)", "辦公自動化（OA）", "庫存與倉儲（WMS / Inventory）"]
          },
          { category: "行業解決方案", sub: ["教育", "政府與公共事業", "信息技術與軟件", "建築與地產"] }
        ],
        contact: "zhaobo@guanglz.cn",
        cn: true,
        certified: true,
        featured: false
      },
      {
        id: "271",
        name: "深圳市創聯雲科技有限公司",
        location: "中國·深圳",
        description: "聚焦政府與公共事業信息化服務，提供基於 NocoBase 的部署、插件開發與本地化支持。",
        logo: "https://www.chuanglianyun.cn/upload/%E5%88%9B%E8%81%94%E4%BA%91%20logo.png",
        website: "https://www.chuanglianyun.cn/",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "行業解決方案", sub: ["政府與公共事業"] }
        ],
        contact: "jason@chuanglianyun.cn",
        cn: true,
        certified: false,
        featured: false
      },
      {
        id: "199",
        name: "上海翱坤信息科技有限公司",
        location: "中國·上海",
        description: "航空領域綜合工程服務商，內部基於 NocoBase 搭建民航規章的業務流程管理系統。",
        logo: "https://service-cn.nocobase.com/storage/uploads/六邊鷹logo-英字-藍色(copy)-szo4yi.png",
        website: "https://www.aerok.com.cn",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "企業管理系統", sub: ["業務流程管理 (BPM)"] },
          { category: "行業解決方案", sub: ["航空"] }
        ],
        contact: "wu.chaoqian@aerok.com.cn",
        cn: true,
        certified: false,
        featured: false
      },
      {
        id: "93",
        name: "安徽有巢數智信息科技有限公司",
        location: "中國·安徽蕪湖",
        description:
          "建築行業數智化解決方案專家，深度集成 BIM、GIS、IoT 技術，以推動建築行業數智化轉型。僅用 1 周開發出鐵路安全穿透式管理系統並 2 個月交付，創造同類項目最快紀錄。",
        logo: "https://service-cn.nocobase.com/storage/uploads/有巢數智%20主LOGO-nkke01.png",
        website: "http://www.youchaoyun.com",
        services: [
          { category: "服務類型", sub: ["定製開發", "維護與支持", "插件/擴展開發", "產品開發", "系統集成"] },
          {
            category: "企業管理系統",
            sub: ["客戶關係管理 (CRM)", "企業資源計劃 (ERP)", "財務與會計", "人力資源管理 (HRM)", "製造（MES / 訂單）", "辦公自動化（OA）", "項目與協同（PM / 協同）", "庫存與倉儲（WMS / Inventory）"]
          },
          { category: "行業解決方案", sub: ["製造業", "建築與地產"] }
        ],
        contact: "wxf@youchaoyun.com",
        cn: true,
        certified: false,
        featured: false
      },
      {
        id: "38",
        name: "上海旭立信息技術有限公司",
        location: "中國·上海",
        description:
          "專業金融科技和商業數據服務公司，基於 NocoBase 1.5 搭建投行專家庫系統，滿足數據權限及專家信息展示需求。",
        logo: "https://service-cn.nocobase.com/storage/uploads/logo-xnpryg.png",
        website: "http://www.stander-info.com",
        services: [
          { category: "服務類型", sub: ["部署（雲/本地化）", "數據遷移", "產品開發", "系統集成", "技術諮詢"] },
          { category: "企業管理系統", sub: ["客戶關係管理 (CRM)", "電商/零售（會員/分銷等）", "人力資源管理 (HRM)"] },
          { category: "行業解決方案", sub: ["金融", "政府與公共事業", "零售/電商"] }
        ],
        contact: "lifeye.yu@stander-info.com",
        cn: true,
        certified: false,
        featured: false
      },
      {
        id: "43",
        name: "江蘇蘭之天軟件技術有限公司",
        location: "中國·江蘇",
        description: "為中小型製造企業提供全場景數字工廠解決方案；基於 NocoBase 構建可擴展的 WMS 倉儲管理系統。",
        logo: "https://service-cn.nocobase.com/storage/uploads/資源 8-xp5mv0.png",
        website: "http://www.orchisky.com/",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "企業管理系統", sub: ["庫存與倉儲（WMS / Inventory）"] },
          { category: "行業解決方案", sub: ["製造業"] }
        ],
        contact: "haifeng.feng@orchisky.com",
        cn: true,
        certified: false,
        featured: false
      },
      {
        id: "78",
        name: "三興堂（廣州）信息技術有限公司",
        location: "中國·廣州",
        description:
          "專注為大灣區中大型企業提供數字化轉型諮詢與定製化服務，深耕地產、零售、製造等行業，並服務多家500強。",
        logo: "https://service-cn.nocobase.com/storage/uploads/3iwom%20logo-x39lp5.jpg",
        website: "https://www.3iwom.com",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          {
            category: "企業管理系統",
            sub: ["業務流程管理 (BPM)", "客戶關係管理 (CRM)", "電商/零售（會員/分銷等）", "企業資源計劃 (ERP)", "財務與會計", "人力資源管理 (HRM)", "製造（MES / 訂單）", "辦公自動化（OA）", "項目與協同（PM / 協同）", "地產/物業管理", "庫存與倉儲（WMS / Inventory）"]
          },
          { category: "行業解決方案", sub: ["政府與公共事業", "醫療健康", "信息技術與軟件", "製造業", "媒體與娛樂", "建築與地產", "零售/電商"] }
        ],
        contact: "forest@3iwom.com",
        cn: true,
        certified: false,
        featured: false
      },
      {
        id: "81",
        name: "湖南飛域無界科技有限公司",
        location: "中國·湖南長沙望城區",
        description:
          "為企業提供一體化的零代碼平臺研發與實施服務，幫助客戶實現業務流程可視化、管理數字化、創新敏捷化。",
        logo: "https://service-cn.nocobase.com/storage/uploads/微信圖片_20250615231905_副本_副本-zto0bj.png",
        website: "http://mc1122.wicp.net:8088/",
        services: [
          { category: "服務類型", sub: ["部署（雲/本地化）", "定製開發", "維護與支持", "產品開發", "系統集成"] },
          {
            category: "企業管理系統",
            sub: ["預約與排期系統", "業務流程管理 (BPM)", "內容管理系統 (CMS)", "客戶關係管理 (CRM)", "電商/零售（會員/分銷等）", "教育（LMS / 學員）", "企業資源計劃 (ERP)", "財務與會計", "醫療（EMR / 患者管理）", "人力資源管理 (HRM)", "製造（MES / 訂單）", "非營利組織（捐贈/活動）", "辦公自動化（OA）", "項目與協同（PM / 協同）", "地產/物業管理", "庫存與倉儲（WMS / Inventory）"]
          },
          { category: "行業解決方案", sub: ["教育", "金融", "政府與公共事業", "醫療健康", "信息技術與軟件", "製造業", "媒體與娛樂", "建築與地產", "零售/電商"] }
        ],
        contact: "249413181@qq.com",
        cn: true,
        certified: false,
        featured: false
      },
      {
        id: "1000159",
        name: "Purple Bridge Sdn Bhd",
        location: "馬來西亞·吉隆坡",
        description: "專注自動化諮詢與解決方案開發，通過定製化方案提升生產力與效率。",
        logo: "https://purplebridge.com.my/wp-content/uploads/2025/05/logor-1.png#149",
        website: "https://www.purplebridge.my",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "企業管理系統", sub: ["業務流程管理 (BPM)", "財務與會計", "項目與協同（PM / 協同）", "庫存與倉儲（WMS / Inventory）"] },
          { category: "行業解決方案", sub: ["金融", "醫療健康", "製造業", "零售/電商"] }
        ],
        contact: "charles@purplebridge.my",
        certified: true,
        featured: false
      },
      {
        id: "1000102",
        name: "RakuCloud株式會社",
        location: "日本·東京",
        description: "為中小企業與創業公司提供基於 NocoBase 的賬單管理、審批與通知工作流解決方案。",
        logo: "https://static.wixstatic.com/media/aa943a_bfc9ee216d94418e9ed31b967d0f8444~mv2.png",
        website: "https://www.rakucloud.co.jp/",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "企業管理系統", sub: ["客戶關係管理 (CRM)", "賬單管理"] },
          { category: "行業解決方案", sub: ["數字廣告", "信息技術與軟件", "製造業"] }
        ],
        contact: "jia@rakucloud.com",
        certified: false,
        featured: false
      },
      {
        id: "1000113",
        name: "ハウスウェル株式會社",
        location: "日本·埼玉縣さいたま市大宮區",
        description: "為不動產行業及其他行業提供業務改善與數字化轉型（DX）解決方案，從現場視角設計、構建和運維。",
        logo: "https://housewell-group.jp/assets/image/common/header/logo.webp",
        website: "https://housewell-group.jp/",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "企業管理系統", sub: ["內容管理系統 (CMS)", "客戶關係管理 (CRM)", "人力資源管理 (HRM)", "地產/物業管理"] },
          { category: "行業解決方案", sub: ["建築與地產"] }
        ],
        contact: "nakazawa@housewell.co.jp",
        certified: false,
        featured: false
      },
      {
        id: "",
        name: "Appnicorn Sdn Bhd",
        location: "馬來西亞·吉隆坡",
        description: "專注於模塊化系統與低代碼解決方案的技術團隊，擅長基於 NocoBase 的系統實施與定製開發。",
        logo: "https://service-cn.nocobase.com/storage/uploads/cropped-Appnicorn_CompanyLogo-9zbmlc.png",
        website: "https://appnicorn.com",
        services: [
          { category: "服務類型", sub: ["部署（雲/本地化）", "定製開發", "數據遷移", "維護與支持", "插件/擴展開發", "系統集成", "技術諮詢"] },
          { category: "企業管理系統", sub: ["客戶關係管理 (CRM)", "企業資源計劃 (ERP)", "財務與會計", "項目與協同（PM / 協同）", "庫存與倉儲（WMS / Inventory）"] },
          { category: "行業解決方案", sub: ["教育", "政府與公共事業", "製造業", "零售/電商"] }
        ],
        contact: "wing@appnicorn.com.my",
        certified: false,
        featured: false
      }
    ]
  },
  {
    continent: "歐洲",
    partners: [
      {
        id: "1000128",
        name: "NIT Solutions B.V.",
        location: "荷蘭·Gorinchem",
        description: "專注構建契合業務流程的 CRM 系統，提供諮詢與實施，並在上線後持續運維與優化。",
        logo: "https://nitsolutions.b-cdn.net/wp-content/uploads/2025/01/logo-nit-solutions.png",
        website: "https://nit.solutions",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持"] },
          { category: "企業管理系統", sub: ["客戶關係管理 (CRM)"] },
          { category: "行業解決方案", sub: ["製造業", "建築與地產", "零售/電商"] }
        ],
        contact: "dion@nitsolutions.nl",
        certified: false,
        featured: false
      }
    ]
  },
  {
    continent: "南美洲",
    partners: [
      {
        id: "1000177",
        name: "ED",
        location: "巴西·里約熱內盧",
        description:
          "ED 擁有 120 餘名顧問，擅長多種技術棧，為教育、金融、政府、醫療等領域提供數字化管理與業務效率提升。",
        logo: "https://ed.dev.br/data/files/ED/B2/BC/58/5FC2091051FD7009600808A8/logo-ed.png",
        website: "https://ed.dev.br",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持", "插件/擴展開發"] },
          {
            category: "企業管理系統",
            sub: [
              "預約與排期系統",
              "業務流程管理 (BPM)",
              "內容管理系統 (CMS)",
              "客戶關係管理 (CRM)",
              "電商/零售（會員/分銷等）",
              "教育（LMS / 學員）",
              "財務與會計",
              "醫療（EMR / 患者管理）",
              "製造（MES / 訂單）",
              "項目與協同（PM / 協同）",
              "地產/物業管理",
              "庫存與倉儲（WMS / Inventory）"
            ]
          },
          {
            category: "行業解決方案",
            sub: ["教育", "金融", "政府與公共事業", "醫療健康", "信息技術與軟件", "製造業", "媒體與娛樂", "建築與地產", "零售/電商"]
          }
        ],
        contact: "leandro.postaue@ed.dev.br",
        certified: false,
        featured: false
      }
    ]
  },
  {
    continent: "大洋洲",
    partners: [
      {
        id: "1000158",
        name: "Engineering Business Services Limited",
        location: "瓦努阿圖·維拉港（Port Vila）",
        description:
          "EBS 自 2015 年起紮根瓦努阿圖，提供從系統設計到運維支持的全流程 IT 服務，含 SLA 合約與 7×24 支持。",
        logo: "https://ebs-vanuatu.com/wp-content/uploads/2021/05/EBS-LOGO-seul-smol-soml.png",
        website: "https://ebs-vanuatu.com",
        services: [
          { category: "服務類型", sub: ["部署實施", "定製開發", "技術支持", "維護與支持"] },
          { category: "企業管理系統", sub: ["業務流程管理 (BPM)", "內容管理系統 (CMS)", "電商/零售（會員/分銷等）", "項目與協同（PM / 協同）"] },
          { category: "行業解決方案", sub: ["金融", "信息技術與軟件"] }
        ],
        contact: "arnaud@ebs-vanuatu.com",
        certified: false,
        featured: false
      }
    ]
  }
];

// ✅ 統一用標準化函數，避免空格/全角/括號說明造成誤判
const normalize = (s: string) => (s || "").trim();

const filterCategoriesMap = {
  服務類型: new Set<string>(),
  企業管理系統: new Set<string>(),
  行業解決方案: new Set<string>(),
};

partnersByContinent.forEach((region) => {
  region.partners.forEach((partner) => {
    partner.services.forEach((service) => {
      const cat = normalize(service.category);

      // 只把“企業管理系統 / 行業解決方案”的【子項】放入對應分組
      if (cat === "企業管理系統") {
        service.sub.forEach((sub) => {
          const item = normalize(sub);
          if (item) filterCategoriesMap["企業管理系統"].add(item);
        });
        return;
      }

      if (cat === "行業解決方案") {
        service.sub.forEach((sub) => {
          const item = normalize(sub);
          if (item) filterCategoriesMap["行業解決方案"].add(item);
        });
        return;
      }

      // 其他一律歸為「服務類型」：類別本身 + 它的子項
      if (cat && cat !== "服務類型") {
        filterCategoriesMap["服務類型"].add(cat);
      }
      service.sub.forEach((sub) => {
        const item = normalize(sub);
        if (item) filterCategoriesMap["服務類型"].add(item);
      });
    });
  });
});

// 轉數組（並去掉空項，確保不會出現“行業解決方案/企業管理系統”自己作為子項）
const filterCategories = Object.entries(filterCategoriesMap).map(([title, set]) => ({
  title,
  items: Array.from(set).filter((it) => it && it !== title),
}));

// 側邊欄顯示用：每個“子項”(如 部署實施/定製開發/技術支持/CRM/ERP/…) 的夥伴數量
const itemCountMap: Record<string, number> = {};
partnersByContinent.forEach((region) => {
  region.partners.forEach((p) => {
    const seen = new Set<string>(); // 同一夥伴內同一子項只計一次
    p.services.forEach((s) => {
      s.sub.forEach((raw) => {
        const sub = (raw || "").trim();
        if (!sub || seen.has(sub)) return;
        seen.add(sub);
        itemCountMap[sub] = (itemCountMap[sub] || 0) + 1;
      });
    });
  });
});

const countries = Array.from(
  new Set(
    partnersByContinent.flatMap((region) =>
      region.partners.map((p) => p.location.split("·")[0]),
    ),
  ),
);

// 扁平化所有 services，並統計次數
const serviceCountMap = {};
partnersByContinent.forEach((region) => {
  region.partners.forEach((p) => {
    p.services.forEach((s) => {
      serviceCountMap[s] = (serviceCountMap[s] || 0) + 1;
    });
  });
});
---

<Layout title={title} description={description} keywords={keywords}>
  <PageHeader title={title} description={description} />

  <!-- Start Partners -->
  <section class="section">
    <div class="container">
      <div class="row">
        <!-- 側邊欄 -->
        <div class="col-lg-2 col-md-3">
          <div class="card border-0 rounded sticky-bar" style="z-index: 1;">
            <div>
              <h6 class="text-dark">快速導航</h6>
              <ul class="list-unstyled sidebar-nav-list mb-4">
                {partnersByContinent.map((region) => (
                  <li>
                    <a href={`#${region.continent}`} class="text-muted">
                      <i class="uil uil-globe me-2"></i>
                      {region.continent}
                    </a>
                  </li>
                ))}
              </ul>

              {/* Service Type filter section */}
              {filterCategories.map((category) => (
                <div class="filter-section">
                  <h6 class="filter-title">
                    <span>{category.title}</span>
                    {(category.title === "企業管理系統" ||
                      category.title === "行業解決方案") && (
                      <i
                        class="uil uil-info-circle info-right text-muted small sub-alarm"
                        data-bs-toggle="tooltip"
                        data-bs-placement="top"
                        title={
                          category.title === "企業管理系統"
                            ? "已有成熟產品，可直接交付與部署"
                            : "有行業方案與實施經驗，可按需定製開發"
                        }
                      ></i>
                    )}
                  </h6>

                  <div class="filter-options">
                    {category.items.map((item) => {
                      const count = itemCountMap[item] || 0;
                      return (
                        <a
                          role="button"
                          class="filter-link"
                          data-filter-type="service"
                          data-filter-value={item}
                          data-active="false"
                        >
                          <input type="checkbox" value={item} style="margin-right: 8px;" />
                          {item} <span class="filter-count">({count})</span>
                        </a>
                      );
                    })}
                  </div>
                </div>
              ))}

              <div class="mt-4">
                <a href="#become-partner" class="btn btn-primary btn-sm w-100">成為合作伙伴</a>
              </div>
            </div>
          </div>
        </div>
        {/* end col */}

        <!-- 主要內容區 -->
        <div class="col-lg-9 col-md-8">
          <div class="ms-lg-4">
            <div class="mb-4 d-flex justify-content-between align-items-start">
              <p class="text-muted mb-0 me-3">
                我們與全球優秀的服務夥伴合作，為您提供專業的 NocoBase 實施、定製開發、技術支持和培訓服務。無論您在世界的哪個角落，都能找到就近的專業夥伴為您提供幫助。
              </p>

              <a id="nocobase"
                href="https://service-cn.nocobase.com/public-forms/ivcvb4f8lpe?partnerId=nocobase"
                class="btn btn-primary btn-sm btn-help text-nowrap"
                data-bs-toggle="tooltip"
                data-bs-placement="left"
                title="如果不確定選擇哪位合作伙伴，或想直接諮詢我們，請點擊這裡">
                <i class="uil uil-message me-1"></i>
                諮詢
              </a>
            </div>

            {/* 搜索、統計和排序 */}
            <div class="partners-header mb-4 sticky-search" style="z-index: 1;">
              <div class="row align-items-center g-3">
                {/* 左側搜索框 */}
                <div class="col-lg-5 col-md-6 col-12">
                  <div class="search-container">
                    <div class="position-relative">
                      <input
                        type="text"
                        class="form-control"
                        placeholder="搜索合作伙伴..."
                        id="partnerSearch"
                        style="padding-left: 40px;"
                      />
                      <i
                        class="uil uil-search position-absolute"
                        style="left: 12px; top: 50%; transform: translateY(-50%); color: #6c757d; font-size: 18px;"
                      ></i>
                    </div>
                  </div>
                </div>

                <div class="col-lg-4 col-md-3 col-12 text-center"></div>

                {/* 國家篩選 */}
                <div class="col-lg-3 col-md-3 col-12 text-end">
                  <div class="plugins-sort">
                    <select class="form-select form-select-sm">
                      <option value="">選擇國家</option>
                      {countries.map((country) => (
                        <option value={country}>{country}</option>
                      ))}
                    </select>
                  </div>
                </div>
              </div>
            </div>

            {/* Partners Content */}
            {partnersByContinent.map((region) => (
              <div class="mt-4" id={region.continent}>
                <h4 class="text-primary mb-4">
                  <i class="uil uil-globe me-2"></i>
                  {region.continent}
                </h4>

                <div class="row">
                  {region.partners.map((partner) => {
                    // 處理官網按鈕的顯示與 URL 規範化
                    const raw = partner.website || "";
                    const trimmed = raw.trim();
                    const websiteUrl = trimmed
                      ? (trimmed.startsWith("http://") || trimmed.startsWith("https://")
                        ? trimmed
                        : `https://${trimmed}`)
                      : "";

                    return (
                      <div class="col-lg-6 col-md-6 mb-4">
                        <div
                          class="card border shadow-sm h-100 partner-card w-100"
                          data-name={partner.name}
                          data-country={partner.location.split("·")[0]}
                          data-services={encodeURIComponent(
                            JSON.stringify(partner.services.flatMap((s) => s.sub.map((x) => x.trim()))),
                          )}
                        >
                          <div class="card-body">
                            <div class="mb-4">
                              <div class="d-flex align-items-center justify-content-between mb-3">
                                {partner.name === "Purple Bridge Sdn Bhd" ? (
                                  <div class="partner-logo" style="background: #050d42;">
                                    <img src={partner.logo} alt={partner.name} loading="lazy" />
                                  </div>
                                ) : (
                                  <div class="partner-logo">
                                    <img src={partner.logo} alt={partner.name} loading="lazy" />
                                  </div>
                                )}

                                {partner.certified && (
                                  <span
                                    class="badge bg-soft-success text-success"
                                    data-bs-toggle="tooltip"
                                    data-bs-placement="top"
                                    title="已通過 NocoBase 官方認證，可提供官方認可的專業服務"
                                  >
                                    <i class="uil uil-check-circle me-1"></i>
                                    認證
                                  </span>
                                )}
                              </div>

                              <div class="mt-3">
                                <h6 class="mb-1">{partner.name}</h6>
                                <small class="text-muted">
                                  <i class="uil uil-map-marker me-1"></i>
                                  {partner.location}
                                </small>
                              </div>
                            </div>

                            <p class="text-muted mb-3">{partner.description}</p>

                            {(() => {
                              const subs =
                                partner.services
                                  .filter((s) => s.category === "服務類型")
                                  .flatMap((s) => (s.sub || []).map((x) => x.trim()))
                                  .filter(Boolean);

                              const uniqueSubs = Array.from(new Set(subs));

                              if (!uniqueSubs.length) return null;

                              return (
                                <div class="mb-3 services-block">
                                  <h6 class="text-dark mb-2">服務範圍：</h6>
                                  <div class="services-tags">
                                    {uniqueSubs.map((item) => (
                                      <span class="badge bg-soft-primary text-primary">{item}</span>
                                    ))}
                                  </div>
                                </div>
                              );
                            })()}

                            <div class="pt-3 border-top card-actions">
                              <div class="d-flex justify-content-between align-items-center">
                                <a
                                  href={`${
                                    partner.cn
                                      ? "https://service-cn.nocobase.com/public-forms/ivcvb4f8lpe"
                                      : "https://service-en.nocobase.com/public-forms/q7pbwgd4xr7"
                                  }?partnerId=${encodeURIComponent(partner.id)}`}
                                  class="btn btn-sm btn-outline-primary"
                                >
                                  <i class="uil uil-envelope me-1"></i>
                                  諮詢該服務
                                </a>

                                {websiteUrl && (
                                  <a href={websiteUrl} target="_blank" class="btn btn-sm btn-soft-primary">
                                    <i class="uil uil-external-link-alt me-1"></i>
                                    官網
                                  </a>
                                )}
                              </div>
                            </div>
                          </div>
                        </div>
                      </div>
                    );
                  })}
                </div>
              </div>
            ))}

            {/* 無結果提示 */}
            <div class="no-results text-center py-5" id="no-result-message" style="display: none;">
              <i class="uil uil-search text-muted" style="font-size: 3rem;"></i>
              <h5 class="mt-3 text-muted">沒有找到符合條件的合作伙伴</h5>
              <p class="text-muted">請嘗試調整搜索條件</p>
            </div>
          </div>
        </div>
        {/* end col */}
      </div>
      {/* end row */}
    </div>
    {/* end container */}
  </section>
  {/* End Partners */}

  {/* Become Partner Start */}
  <section class="section bg-light" id="become-partner">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-12">
          <div class="section-title text-center mb-4 pb-2">
            <h4 class="title mb-4">成為 NocoBase 合作伙伴</h4>
            <p class="para-desc mx-auto text-muted mb-0">
              如果您具備專業的服務能力，並希望加入 NocoBase 成為合作伙伴，我們誠摯歡迎您的加入，並將為您提供技術支持、培訓資源與市場推廣等全面支持。
            </p>
          </div>
        </div>
        {/* end col */}
      </div>
      {/* end row */}

      <div class="row justify-content-center">
        <div class="col-lg-10">
          <div class="card bg-light border-0">
            <div class="card-body">
              <h6 class="text-dark mb-4 text-center">合作伙伴權益</h6>
              <div class="row justify-content-center">
                <div class="col-lg-3 col-md-4 text-center">
                  <ul class="list-unstyled text-muted mb-0">
                    <li class="mb-2 d-flex align-items-center justify-content-start">
                      <i class="uil uil-check-circle text-success me-2"></i>
                      <span>官方認證與品牌授權</span>
                    </li>
                    <li class="mb-2 d-flex align-items-center justify-content-start">
                      <i class="uil uil-check-circle text-success me-2"></i>
                      <span>技術培訓和認證體系</span>
                    </li>
                    <li class="mb-2 d-flex align-items-center justify-content-start">
                      <i class="uil uil-check-circle text成功 me-2"></i>
                      <span>官網展示與品牌曝光</span>
                    </li>
                  </ul>
                </div>

                <div class="col-lg-3 col-md-4 text-center">
                  <ul class="list-unstyled text-muted mb-0">
                    <li class="mb-2 d-flex align-items-center justify-content-start">
                      <i class="uil uil-check-circle text-success me-2"></i>
                      <span>市場推廣資源支持</span>
                    </li>
                    <li class="mb-2 d-flex align-items-center justify-content-start">
                      <i class="uil uil-check-circle text-success me-2"></i>
                      <span>優先獲得商業機會推薦</span>
                    </li>
                    <li class="mb-2 d-flex align-items-center justify內容-start">
                      <i class="uil uil-check-circle text-success me-2"></i>
                      <span>參與官方生態共建</span>
                    </li>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        </div>
        {/* end col */}
      </div>
      {/* end row */}

      <div class="row justify-content-center">
        <div class="col-12 text-center mt-4">
          <a href="https://service-cn.nocobase.com/public-forms/iv0g603rvzp" class="btn btn-primary me-2">
            <i class="uil uil-user-plus me-1"></i>
            申請成為合作伙伴
          </a>
          <a href="/tw/contact" class="btn btn-outline-primary">
            <i class="uil uil-envelope me-1"></i>
            瞭解更多
          </a>
        </div>
        {/* end col */}
      </div>
      {/* end row */}
    </div>
    {/* end container */}
  </section>
  {/* Become Partner End */}

  <style>
    .partner-logo {
      height: 30px; /* 統一顯示高度，可按需改 40~60px */
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 8px;
      background-color: #f8f9fa;
      border-radius: 8px;
      border: 1px solid #e9ecef;
    }

    .partner-logo img {
      height: 100% !important; /* 關鍵：按容器高度鋪滿 */
      width: auto; /* 寬度自適應，保持比例 */
      max-width: 100%; /* 防止超出容器 */
      object-fit: contain; /* 保持完整可見 */
      transition: opacity 0.3s ease;
    }

    .partner-logo:hover img {
      opacity: 0.8;
    }

    /* 懸浮元素 */
    .sticky-search {
      position: sticky;
      top: 70px;
      z-index: 1000;
      background: #fff;
      padding: 1rem 0;
      border-bottom: 1px solid #e9ecef;
    }

    .sticky-bar {
      position: sticky;
      top: 100px;
      z-index: 99;
    }

    .sidebar-nav-list li a {
      display: flex;
      align-items: center;
      padding: 8px 12px;
      border-radius: 6px;
      transition: all 0.3s ease;
      text-decoration: none;
    }

    .sidebar-nav-list li a:hover {
      background-color: #f8f9fa;
      color: #2f55d4 !important;
    }

    /* 側邊欄篩選器 */
    .plugins-sidebar {
      position: sticky;
      top: 80px;
      height: fit-content;
    }

    /* 篩選選項 */
    .filter-section {
      margin-bottom: 1.5rem;
    }

    .filter-title {
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: #343a40;
      font-size: 1rem;
    }

    .filter-options {
      display: flex;
      flex-direction: column;
      gap: 0.1rem;
    }

    .filter-link {
      display: flex;
      align-items: center;
      padding: 0.3rem 0.5rem;
      border-radius: 3px;
      text-decoration: none;
      font-size: 0.95rem;
      transition: all 0.2s;
    }

    .filter-link input[type="radio"],
    .filter-link input[type="checkbox"] {
      margin-right: 8px;
      flex-shrink: 0;
    }

    .filter-link .filter-count {
      margin-left: auto;
      font-size: 0.8rem;
      opacity: 0.7;
    }

    .filter-link:hover {
      background-color: #f8f9fa;
      text-decoration: none;
    }

    .filter-link[data-active="true"] {
      font-weight: bold;
    }

    .filter-section .form-check {
      margin-bottom: 0.5rem;
    }

    .filter-section .form-check-label {
      font-size: 0.9rem;
      color: #343a40;
    }

    .filter-section .form-check input[type="checkbox"]:checked + .form-check-label {
      font-weight: bold;
      color: #007bff;
    }

    /* 插件內容區域 */
    .plugins-content {
      padding-left: 0;
    }

    .plugin-card {
      transition: transform 0.2s, box-shadow 0.2s;
      height: 100%;
    }

    .plugin-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1) !important;
    }

    .plugin-card .card-body {
      display: flex;
      flex-direction: column;
      height: 100%;
    }

    .plugin-overview {
      font-size: 0.9rem;
      line-height: 1.4;
      word-break: break-word;
    }

    .plugin-box {
      flex: 1;
    }

    .plugin-footer {
      margin-top: 1rem;
    }

    /* 響應式設計 */
    @media (max-width: 768px) {
      .plugins-sidebar {
        position: relative;
        top: auto;
        margin-bottom: 2rem;
      }

      .plugins-content {
        padding-left: 0;
      }

      .sticky-search .col-12 {
        margin-bottom: 0.5rem;
      }

      .sticky-search .col-12:last-child {
        margin-bottom: 0;
      }

      .sticky-search .text-center,
      .sticky-search .text-end {
        text-align: left !important;
      }
    }

    @media (max-width: 991px) {
      .sticky-bar {
        position: relative;
        top: auto;
        margin-bottom: 30px;
      }
    }

    /* 讓卡片主體是“頭部(不伸縮) + 服務區(可伸縮) + 底部(固定在底)” */
    .partner-card .card-body {
      display: flex;
      flex-direction: column;
    }

    .partner-card .services-block {
      flex: 1 1 auto; /* 關鍵：讓這一塊吃掉額外空間 */
      display: flex;
      flex-direction: column;
      min-height: 0; /* 防止與 flex 子項在某些瀏覽器的高度衝突 */
    }

    .partner-card .services-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.25rem; /* badge 間距 */
      align-content: flex-start; /* 多行從上方開始堆疊，下面留白 */
    }

    .partner-card .card-actions {
      margin-top: auto; /* 底部區域貼底 */
    }

    .sub-alarm {
      float: right;
      margin-right: 0.5rem;
    }
  </style>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      // 小工具
      const $ = (s, r = document) => r.querySelector(s);
      const $$ = (s, r = document) => Array.from(r.querySelectorAll(s));
      const norm = (s) => (s || "").normalize("NFKC").trim().toLowerCase(); // 兼容全角/半角

      const headerOffset = 140;

      // 關鍵節點
      const searchInput = $("#partnerSearch");
      const countrySelect = document.querySelector(".plugins-sort select");
      const serviceCheckboxes = $$('[data-filter-type="service"] input[type="checkbox"]');

      // 卡片與其外層列容器
      const cards = $$(".partner-card");
      const cardHosts = new Map(); // card -> 宿主列元素（用於隱藏/測量）
      const cardTitles = new Map(); // card -> 標題文本（僅用於搜索）

      cards.forEach((card) => {
        const host =
          card.closest(".col-lg-6, .col-md-6, .col-sm-12, .col-12, .col-xl-6") || card.parentElement;
        cardHosts.set(card, host);

        // 標題優先取 data-name，兜底取 h6 文本
        const titleFromData = (card.dataset.name || "").trim();
        const h6 = card.querySelector("h6");
        const title = titleFromData || (h6 ? h6.textContent.trim() : "");
        cardTitles.set(card, title);
      });

      // 所有大洲分組容器（帶 id 且內部含有 .partner-card）
      const groups = $$("[id]").filter((el) => el.querySelector(".partner-card"));

      // === 滾動工具 ===
      function scrollToSearch() {
        if (!searchInput) return;
        const top = searchInput.getBoundingClientRect().top + window.pageYOffset;
        window.scrollTo({ top: top - headerOffset - 8, behavior: "smooth" });
      }

      function scrollToFirstVisibleCardOrSearch() {
        // 找到第一個可見卡片的宿主列
        const first = cards.find((card) => {
          const host = cardHosts.get(card);
          return host && host.style.display !== "none";
        });

        if (first) {
          const host = cardHosts.get(first);
          const top = host.getBoundingClientRect().top + window.pageYOffset;
          window.scrollTo({ top: top - headerOffset - 8, behavior: "smooth" });
        } else {
          // 無匹配卡片，滾到搜索框可見位置
          scrollToSearch();
        }
      }

      // 無結果提示
      function updateNoResult() {
        const anyVisible = cards.some((card) => {
          const host = cardHosts.get(card);
          return host && host.style.display !== "none";
        });
        const msg = $("#no-result-message");
        if (msg) msg.style.display = anyVisible ? "none" : "block";
      }

      // 核心過濾：只按“標題”匹配
      function filterCards() {
        const q = norm(searchInput ? searchInput.value : "");
        const selectedCountry = countrySelect ? countrySelect.value : "";
        const selectedServices = serviceCheckboxes.filter((cb) => cb.checked).map((cb) => cb.value);

        cards.forEach((card) => {
          const host = cardHosts.get(card);
          if (!host) return;

          // 僅使用標題參與搜索
          const title = norm(cardTitles.get(card));
          const matchText = !q || title.includes(q);

          // 國家篩選（data-country 來自模板）
          const cardCountry = (card.dataset.country || "").trim();
          const matchCountry = !selectedCountry || cardCountry === selectedCountry;

          // 服務篩選（data-services 裡是 JSON 字符串）
          let services = [];
          try {
            services = JSON.parse(decodeURIComponent(card.dataset.services || "%5B%5D"));
          } catch {}
          const matchServices =
            selectedServices.length === 0 ? true : selectedServices.every((s) => services.includes(s));

          host.style.display = matchText && matchCountry && matchServices ? "" : "none";
        });

        // 組顯隱
        groups.forEach((group) => {
          const hasVisible = $$(".partner-card", group).some((card) => {
            const host = cardHosts.get(card);
            return host && host.style.display !== "none";
          });
          group.style.display = hasVisible ? "" : "none";
        });

        updateNoResult();
      }

      // 側邊欄 filter-link 點擊切換複選框並過濾 + 自動滾動到第一個卡片
      $$(".filter-link").forEach((link) => {
        link.addEventListener("click", (e) => {
          e.preventDefault();
          const cb = link.querySelector('input[type="checkbox"]');
          if (!cb) return;
          cb.checked = !cb.checked;
          link.dataset.active = String(cb.checked);
          filterCards();
          scrollToFirstVisibleCardOrSearch();
        });
      });

      // 側邊欄複選框變化（鍵盤或無障礙操作）同樣滾動
      serviceCheckboxes.forEach((cb) =>
        cb.addEventListener("change", () => {
          filterCards();
          scrollToFirstVisibleCardOrSearch();
        }),
      );

      // 搜索框：只過濾，不自動滾動（保留原體驗）
      if (searchInput) {
        searchInput.addEventListener("input", filterCards);
        searchInput.addEventListener("compositionend", filterCards);
      }

      // 國家篩選（頂部右側）：只過濾，不滾動（不是側邊欄）
      if (countrySelect) {
        countrySelect.addEventListener("change", filterCards);
      }

      // 側邊欄錨點平滑滾動（不觸發自動滾）
      $$('.sidebar-nav-list a[href^="#"]').forEach((link) => {
        link.addEventListener("click", (e) => {
          e.preventDefault();
          const id = link.getAttribute("href").substring(1);
          const group = document.getElementById(id);
          if (!group) return;

          const firstVisible = $$(".partner-card", group).find((card) => {
            const host = cardHosts.get(card);
            return host && host.style.display !== "none";
          });
          if (!firstVisible) return;

          const top =
            (cardHosts.get(firstVisible) || firstVisible).getBoundingClientRect().top + window.pageYOffset;
          window.scrollTo({ top: top - headerOffset, behavior: "smooth" });
        });
      });

      // 初始化
      filterCards();
    });
  </script>
</Layout>
